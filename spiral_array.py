# traverse top, then right then bottom of 2d array
# measure arrays dimeniosn and declare varaibles (starting row, column ending row, column)
# start for room (starting from starting row, to end col), then traverse down the right array
